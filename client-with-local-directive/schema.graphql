type Query {
  characters: CharacterList
}

type CharacterList {
  results: [Character] 
}

type Character {
  id: ID
  name: String
  gender: String
  image: String
  origin: Origin
}

type Origin {
  id: ID
}
