type Query {
  me: User
}

type User {
  firstName: String
  lastName: String
  middleName: String
  name: String
    @deprecated(
      reason: "Not specific enough. Use firstName and lastName instead"
    )
}
